{{!< default}}

<div class="content-area-feed">
    <main class="site-main">
        <div class="post-feed container medium">

    
            {{#get "posts" filter="tag:-[news]+tag:-[rejections,un]"}}
                {{#foreach posts}}
                    {{!-- {{^has tag="news"}}
                        {{^has tag="rejections"}} --}}
                    {{> "loop"}}
                        {{!-- {{/has}} --}}
                    {{!-- {{/has}} --}}
                {{/foreach}}
            {{/get}}
        </div>
        {{pagination}}
    </main>
</div>